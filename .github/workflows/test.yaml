name: Test

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  log_message:
    runs-on: ubuntu-latest
    steps:
      - name: Log a message
        run: echo "This is a test workflow running on push to main or manual trigger."